# Vibe Editing — API keys (auto-loaded). Bring your OWN. Nothing ships with a key.
# Recommended: free Groq key (console.groq.com) for fast transcription.
GROQ_API_KEY=
# Optional: most reliable caption styling (else uses your Claude Code login).
ANTHROPIC_API_KEY=
ELEVENLABS_API_KEY=
